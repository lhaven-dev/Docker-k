apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tp-docker-k8s

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - deployment-with-config-storage.yaml
  - service-clusterip.yaml
  - service-nodeport.yaml
