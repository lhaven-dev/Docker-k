apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-world-data
  namespace: tp-docker-k8s
  labels:
    app: hello-world
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
