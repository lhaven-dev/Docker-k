apiVersion: v1
kind: Service
metadata:
  name: hello-world-clusterip
  namespace: tp-docker-k8s
  labels:
    app: hello-world
spec:
  type: ClusterIP
  selector:
    app: hello-world
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
